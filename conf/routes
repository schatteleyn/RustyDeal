# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET	/band			    controllers.Application.band
GET	/listen			    controllers.Application.listen
GET	/links			    controllers.Application.links
GET	/discography		    controllers.Discography.index
GET	/shows			    controllers.Show.index

GET	/login			    controllers.User.login
POST	/login			    controllers.User.submit

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
